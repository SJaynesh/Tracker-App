import 'package:flutter/material.dart';

class AllSpendingComponent extends StatelessWidget {
  const AllSpendingComponent({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("All Spending Page"),
    );
  }
}
